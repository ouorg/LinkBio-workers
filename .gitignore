# =============================================================================
# LinkBio-workers — Git ignore rules
# 依赖目录、构建产物、本地密钥等不要提交到仓库。
# Dependencies, build artifacts, and local secrets must NOT be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# Dependencies (must NOT upload to the repo)
# 依赖安装目录：体积大、含平台相关二进制，由 package.json / package-lock.json
# 声明即可。克隆后执行 npm ci 或 npm install 本地重建。
# Do NOT commit node_modules. Reinstall with: npm ci
# -----------------------------------------------------------------------------
node_modules/
**/node_modules/

# npm / yarn / pnpm caches (if ever created under the project)
.npm/
.yarn/
.pnpm-store/
.pnpm-debug.log*

# -----------------------------------------------------------------------------
# Build & local runtime (Cloudflare Workers / Wrangler)
# -----------------------------------------------------------------------------
dist/
.wrangler/
.dev.vars
*.tsbuildinfo

# -----------------------------------------------------------------------------
# Environment & secrets (keep local only)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
!.dev.vars.example

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# -----------------------------------------------------------------------------
# OS / IDE
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json

# -----------------------------------------------------------------------------
# Optional: generated types (uncomment if you prefer not to commit them)
# -----------------------------------------------------------------------------
# worker-configuration.d.ts
